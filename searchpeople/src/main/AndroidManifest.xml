<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution">

    <dist:module
        dist:instant="false"
        dist:title="@string/title_searchpeople">
        <dist:delivery>
            <dist:on-demand />
        </dist:delivery>
        <dist:fusing dist:include="true" />
    </dist:module>

    <application
        android:hardwareAccelerated="true"
        android:supportsRtl="true">
        <provider
            android:name="com.example.dinamicfeature.di.initialization.SearchInitialization"
            android:authorities="${applicationId}.searchpeople"
            android:enabled="true"
            android:exported="false" />
    </application>
</manifest>